{
  "description": "workflow-adapter hooks - agent continuation and prompt history logging",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/agent-stop-hook.ps1\""
          }
        ]
      }
    ]
  }
}
