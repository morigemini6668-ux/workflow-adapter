{
  "name": "qa-browse",
  "version": "0.1.0",
  "description": "Headless browser CLI for QA testing — persistent Playwright daemon with ref-based element selection",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/cli.ts --outfile dist/qa-browse && echo 'Built: dist/qa-browse'",
    "dev": "bun run src/cli.ts",
    "server": "bun run src/server.ts",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.58.2",
    "diff": "^7.0.0",
    "tsx": "^4.21.0"
  }
}
